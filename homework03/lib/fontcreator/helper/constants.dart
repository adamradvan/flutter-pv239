import 'package:flutter/material.dart';

@immutable
class AppConstants {
  static const String DEFAULT_MESSAGE = "Hello Flutter!";
  static const double DEFAULT_FONT_SIZE = 8.0;
  static const FontWeight DEFAULT_FONT_WEIGHT = FontWeight.normal;

  AppConstants._();
}
